[
    {
        "method_name": "select_pie_chart_categories",
        "expected_sql": "select type, sum(quantity) as qty from expenses_classified where date_record between '20201201' and '20210101' group by type order by abs(qty) desc",
        "data": {
            "date_from": "20201201",
            "date_to": "20210101"
        }
    }
]
